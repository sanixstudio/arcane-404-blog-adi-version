import {
	chakra,
	Box,
	List,
	ListItem,
	Link
} from '@chakra-ui/react'
// import { Link } from 'react-router-dom'

export const NavBarBox = chakra(Box, {})
export const NavBarList = chakra(List, {})
export const NavBarItem = chakra(ListItem, {})
export const NavBarLink = chakra(Link, {})
